
<div>
  <% @books.each do |book| %>
  <div>
    <ul>
      <%= image_tag book.image if book.present? %>
      <li><%= book.title %></li>
      <li><%= book.author %></li>
      <li><%= book.page_count %></li>
    </ul>
  </div>
  <% end %>
</div>