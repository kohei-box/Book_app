<% if user_signed_in? %>
  <%= render 'shared/review_form' %>
  
    <%= render 'reviews/review'%>

<% else %>
  <h1>Welcome!!</h1>
  <p>ここはアプリのホームです。</p>
  <p>ここではアプリケーション紹介と、ログインを行います。</p>
<% end %>